{
  "name": "bengaluru-residences-project-library",
  "version": "1.0.0",
  "private": true,
  "description": "Static project library website for sales presentations — Bengaluru real-estate projects.",
  "scripts": {
    "build": "node scripts/generate.js",
    "start": "npm run build && npx serve dist"
  }
}
